# Phase 17.1 authorization and implementation handoff

This is a reviewed proposal, not an executable Windows training kickoff. The
owner has authorized diagnostic review and preparation only. **Do not train.**

Read `AGENTS.md`, `PROJECT_STATE.md`, the locally supplied private context,
`experiments/recovery-diagnostic/RETURN_REVIEW.md`, `RETURN_VERIFICATION.json`,
and `experiments/phase-17-1/PROPOSAL.md` plus `plan.json`.

The bounded RTX 4070 SUPER synthetic diagnostic passed its frozen rule: legacy
continuation exceeded `1e-5`, while all 485 deterministic replay updates matched
full states and losses exactly. Its measured source is
`0e5b55f516b6dd53a48f53928df2616872ca5f82`; returned ZIP SHA-256 is
`340b57d7f201436e47b575b7a792cb208cb74dfe47cd5f835322d313304c47d9`.
Mac verified identities, inventories, state records, schedules, exposure, resources
and the prespecified decision. The separately documented Mac cross-host schedule
verification limitation did not change the CUDA loss/state gates.

Phase 17 remains permanently failed; every prior learned/synthetic model is
ineligible to initialize this new attempt. No exact historical kernel diagnosis
or learned capability follows from the diagnostic.

The proposed authorization scope is one fresh seed-160, 34,087,424-parameter
Base Model 2.0 attempt on accepted Phase 15 data under the Phase 16 recipe:
7,485 logical updates / 37,811,418 targets, CUDA BF16, the exact tested deterministic
policy, separately bound checkpoint policy, append-only supervisor, two-hour
training/preflight/recovery cap plus one-hour cumulative fixed evaluation reserve,
20 GiB new-artifact cap, and unchanged acceptance gates. One seed/pass/attempt;
no search, no automatic retry. See the plan for all stop conditions and the two
conditional same-attempt interruption allowances.

Only after separate explicit owner authorization: implement and separately review
the original standalone Phase 17.1 controller described in the proposal. Keep
the native core byte-identical; freeze the new controller, tiny conformance fixtures,
source commit and allowlisted transfer hash before Windows execution. Reject
missing authorization, mismatched identities/policy and existing attempt roots.
Run the specified zero-update checks and at most 52 tiny CUDA preflight updates
on Windows; then the one serious attempt if all prerequisites pass. No full-run
controller or executable transfer is claimed to exist at this proposal checkpoint.

Windows uses its preserved original pinned runtime and data but new Phase 17.1
tools/results roots. Do not use the old Phase 17 launcher, recovery launcher or
diagnostic runner as a full-training command. Do not overwrite historical source,
data, ledgers, tensors or completed diagnostic evidence. Return paired development
evidence before any final acceptance; Mac must independently review and issue the
required candidate/contamination declaration after all development/resource gates.

There is no remote Windows connection from this Mac. The owner transfers the
future checked execution bundle to a fresh Windows Work session and returns its
evidence to this authoritative Mac task. Do not ask the owner to design technical
settings or perform the engineering review.

At this checkpoint, wait for authorization. No full training, Phase 18 or publication.
