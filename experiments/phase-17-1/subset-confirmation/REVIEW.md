# Separate final-confirmation implementation review

The owner authorized the final independent-seed fixed-subset confirmation and a
Windows execution checkpoint. This Mac pass reviewed the changed controller,
worker, evidence handling, hard deadline and terminal policy against that scope.
It is not a new learned result or Windows execution claim.

## Reviewed implementation

The new tools are isolated under `subset-confirmation/` and write only the new
`subset-confirmation-v1` Windows attempt root. Previous source, models, reports,
failures, correction and gates remain unchanged. The native LatoS implementation,
selected tokenizer, exact cache, dependency lock, numerical controls and Phase 5
reference are retained. No new dependency, data or acquired weights are introduced.

The bounded change is D/E, both four-layer/21,238,272 parameters with fresh seed 161:
764 versus 1,528 updates, 19-update warmup and declared short/long cosine horizon.
D2/E2/E4 are the only experimental fixed-suite observations. Four pass tails are
flushed, including five microbatches and the last single window. Empty initial
optimizers, identical D/E initial tensors/samplers, common first-19-update warmup
and every seed-161 input signature are checked in saved-evidence reconstruction.
The existing 52-update fixture set remains separate, including its frozen fixture
seeds, and never initializes D/E. Maximum physical work is 2,344 updates.

The guardian's previously validated process-tree mechanism is retained with the
new attempt identity and 1,795-second cutoff. The controller uses 1,790 seconds,
with 180/1,080/300/180-second cumulative stage caps. The kickoff captures QPC before
Python startup. Packaging and readback are inside the same budget. The return reader
rejects an end-to-end time above 1,800 seconds. There is no 45-minute fallback,
clock reset, retry, resumed training or automatically authorized corrective run.

The corrected comparison logic is incorporated directly: three primary gate
comparisons use Phase 5; three descriptive contrasts use the verified saved-row
bootstrap. No cross-arm model is passed to the base-acceptance function. Complete
successful-path packaging is now tested with real historical saved-score fixtures,
not only scripted process receipts. Those fixtures are not seed-161 results.

The terminal policy is machine-readable and tested for both outcomes. E4 failure
requires `stop/rethink-data-training`; an E4 pass requires owner review only.
Both set further fixed-subset exposure, base acceptance and Phase 18 authorization
false. D2/E2 cannot substitute for E4. Improving loss never authorizes another pass.
An incomplete run is preserved as incomplete, not assigned an invented gate score.

## Validation and review findings

Twenty-seven focused checks pass in the development and existing installed-wheel
runtimes, and again with the extracted sealed handoff tools in both runtimes.
Trainer/AdamW updates, model forwards, evaluator execution and CUDA initialization
are prohibited in the tests. Tiny random CPU models are used only for storage
readback; all long traces are explicitly scripted accounting records. Tests cover
all four pass boundaries, strict configuration/seed/counts, schedule contrast,
checkpoint storage, tampering, timeouts, mocked Windows Job lifecycle, fixed job
order, real saved-row comparison/packaging, invalid reference rejection, terminal
stop behavior and rejection of a return above 30 minutes.

The separate review identified Python module-name collisions as a maintenance risk
when collecting new and historical tests together. New operational modules use the
`cf_` namespace; historical `ss_` modules stay unchanged. All **66 zero-update checks**
from the old subset, its packaging correction and the new confirmation also pass
in one process. No broad optimizer-bearing historical suite was run.

Lint/format, JSON/link, privacy, source-preservation and cache/hash checks pass.
The provisional 24-member bundle passed allowlist/readback verification. Exact final
Git/source binding and corruption rejection are checked after the reviewed local
commit and recorded with the external handoff receipt. See [validation.json](validation.json).

No Mac training, new model inference/scoring, CUDA execution, hosted CI, physical
Linux validation, publication or paid resources occurred. Actual seed-161/four-pass
CUDA execution, runtime and learned quality remain pending; prior measurements are
projections, not a guarantee this run completes within budget. The required actual
Windows guardian/conformance checks must pass inside preparation or the attempt ends.

**Disposition:** ready for the requested Windows handoff only. Preserve permanent
Phase 17 and Phase 17.1 failures, the earlier diagnostic, and all unaccepted models.
Stop at the Windows execution checkpoint; no Phase 18 or publication.
