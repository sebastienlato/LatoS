# Separate final-confirmation closure review

The owner supplied actual return/receipt/completion/guardian files. A separate Mac
review pass followed the first reconstruction, with no delegation or model execution.

- The reviewed local `verify_return.py` rehashes the 302,819,663-byte return, all
  1,421 included payloads, the known handoff and timing receipts; validates the
  returned source manifest; reconstructs 2,292 input signatures, four-pass tails,
  state digests, frozen configuration/seed/global RNG, initial D/E equality and
  common warmup; accounts for 52 separate conformance updates and 15 executed native
  snapshots; and reproduces all primary and descriptive comparisons exactly.
- `audit_closure.py` provides supplementary Git/source binding, strict job ordering,
  stage and whole-clock checks, monitor identities, per-pass arithmetic, independent
  tensor-storage inspection and cross-study descriptive comparisons. D2/E2/E4's
  90 included float32 tensors (63,714,816 elements) are finite and match logged
  fingerprints. Initializers are new relative to seed 160 by recorded identity;
  omitted initial/optimizer tensor bytes are not numerically reconstructed on Mac.
- All 2,292 actual rates equal their recorded Windows schedules exactly. Fifty-seven
  analytic Mac recomputations differ by up to three output ULPs, all within the
  preexisting adjacent-libm-cosine propagation method. This is separate analytic
  compatibility, not a relaxation of same-host rates, states, loss or quality gates.
- The native reference guard remains unchanged. Current reference quality aggregates
  equal the prior reference; resource timing is not required to repeat exactly.
  Evaluator manifests remain development-only. No final/access declaration, model
  promotion or checkpoint substitution is inferred from successful execution.
- The stopping rule is reproduced as `stop/rethink-data-training`, with E4 failing
  eight mandatory gates. It is applied regardless of D2→E4 relative improvement or
  falling in-domain loss. E2 does not replace E4. The planning analysis proposes no
  more passes, seeds or exposure-only experiment on the closed subset.

The initial supplementary audit assumed the return included its `tools/` payloads.
That packaging format omits them; the returned manifest is instead bound to the
known original handoff. The audit was corrected to compare the verified known-bundle
files to Git and use the separately returned actual native-source snapshots. The
first assertion failed before any model/tensor execution; it was a local path
assumption, not a Windows failure or scientific exception. No frozen contract changed.

Both completed verification passes, source/contract preservation, arithmetic,
JSON/link, privacy, lint and formatting checks pass. No broad optimizer-bearing
test suite was run. No new optimizer update, model construction/forward/generation,
new model scoring, CUDA/physical Linux execution, CI, data acquisition or publication
occurred. The private external context was refreshed as documentation only; no
external source, data, weights, dimensions or recipe were imported into the project.

Limits: 37 omitted tensors remain inventory identities on Windows; this is not a
full backup or independent CUDA rerun. Platform/resource evidence is scoped to the
supervised jobs. Two small-model seeds and one depth comparison cannot establish
broad generalization, seed robustness or causal domain/optimizer diagnoses. The
closed experiments do establish that these fixed-budget recipes did not pass the
unchanged gates. The prospective stop is now authoritative and must not be reopened.
