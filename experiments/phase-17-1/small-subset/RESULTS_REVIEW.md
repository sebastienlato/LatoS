# Separate small-subset result review

2026-09-25. Performed within this Mac task, without delegation or model execution.

1. Ran the already reviewed corrective-return reader against the actual transferred
   ZIP, receipt, guardian, original handoff and corrective-script receipt. Verified
   1,667 payloads/manifest membership, 43 declared omissions, original failure
   records, both cumulative budgets, preservation inventory, all serious inputs/
   states/checkpoints and saved comparisons. No returned code was executed.
2. A separate supplementary audit binds original bundle payloads and corrective
   source directly to their reviewed Git commits; checks every serious configuration,
   seed, empty initial optimizer, monitoring split, finite recorded metrics, time
   ordering and stage bounds; verifies four included model tensors as finite float32
   with exact per-tensor fingerprints and counts. No model was instantiated or run.
3. Confirmed identical A/B initial tensors/samplers and exact first-19-update warmup
   model/optimizer/sampler/RNG fingerprints, exact B/C Windows schedules and matched
   input streams. Reconstruction covers 1,910 serious updates; the original four
   synthetic replay pairs account separately for 52 updates. Correction adds zero.
4. Rebuilt all four primary comparisons against the fixed Phase 5 reference and
   all prespecified descriptive contrasts from saved rows. All endpoints fail the
   same eight development gates. Reference quality aggregates and generated IDs
   exactly match the prior pinned Windows reference. No favorable checkpoint or
   result was substituted, and no final/reserved payload was scored.
5. Independently recalculated quality deltas, per-prompt repetition directions and
   full-job timing. Separate relative early-learning benefit from gate failure;
   separate cheaper execution from quality superiority; retain single-seed and
   exposure/schedule/domain limitations. The proposed next study is planning only,
   costs from measured jobs rather than update-only throughput, and has a finite
   stop rule plus a cap below the owner's 45-minute maximum.

Two preliminary supplementary assertions were corrected transparently. A one-output-
ULP cross-host schedule check was too narrow: 59 Mac/Windows output values differ,
by at most three output ULPs. All are explained by the **preexisting** adjacent-libm-
cosine propagation method in `experiments/recovery-diagnostic/review_return.py`.
All 1,910 logged rates exactly equal their recorded Windows schedules, and B/C
schedules are exactly equal; no same-host rate, state, loss or acceptance gate was
relaxed. The second preliminary check compared whole reference summaries including
elapsed resource timing; the timing legitimately differs. Restricting that check
to quality aggregates and generated IDs confirms exact agreement. These were local
supplementary-verifier assumptions, not Windows study failures or protocol changes.

The completed corrective reader and supplementary audit both pass. Lint, format,
JSON/link checks, privacy inspection and historical-preservation checks pass. No
broad optimizer-bearing test suite, training, inference, generation, new model
scoring, data acquisition, GPU use, CI, physical Linux or publication was performed.
Original plans, executed tools, gates, dependency lock and historical reports remain
unchanged. New files are this review, derived results and a read-only verifier.

Limit: Mac can rehash included original files and four included model files; 43
omitted tensor/cache/bytecode records remain inventory identities. Their before/
after preservation is supported by the exact executed corrective code and its
passed preservation record, not a new Mac rehash of absent payloads. This is not a
complete backup or an independent CUDA rerun. The study establishes no accepted
Base Model 2.0. Phase 18 and further experiments/publication remain unauthorized.
