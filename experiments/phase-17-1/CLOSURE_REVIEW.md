# Separate Phase 17.1 closure review

A separate Mac review pass followed initial archive/execution reconstruction and
development comparison reconstruction. It was performed within this task; no
external reviewer, returned code execution, CUDA replay or new model scoring is
claimed. The experiment's native code, controllers, plan and gates are unchanged.

- Rehash the supplied ZIP and receipt, every included member and extracted file;
  bind controller payloads to the original Mac handoff and exact reviewed Git
  commit. Bind twelve executed native snapshots to the unchanged implementation.
  Compare per-job inventories against included plus declared omitted records.
- Keep production namespaces separate from CPU test fixtures. Failed and interrupted
  *scripted* fixture receipts in test artifacts are not real production attempts.
  The actual supervisor has one initial session and the exact 13-job sequence:
  integrity, two CPU suites, eight tiny CUDA workers, training and development.
- Strengthen the second pass to check worker request/import/runtime identity,
  exact selected model and optimizer settings, independently generated synthetic
  fixture inputs, fresh initialization records and each checkpoint's counters/kind.
  This avoids relying only on a top-level status or comparison receipt.
- Reconstruct all 7,485 serious input signatures from the accepted Mac cache and
  fixed permutation, plus exposure, tail accumulation, sampler state, optimizer
  group hashes and exact Windows schedule bindings. Recompute every full-state
  record digest. No cross-platform schedule-rounding allowance is substituted
  for exact equality within the recorded Windows run.
- Check all nine checkpoint policy/readback records against their corresponding
  training states, native metadata and tensor inventories. Rehash included final
  weight bytes and compare all final tensor fingerprints/finite float32 values
  without constructing or running a model. Absent optimizer/intermediate tensors
  remain inventory identities, not independently rehashed or replayed bytes.
- Rebuild fixed development aggregates, behavior observations, choice predictions,
  gate checks and paired intervals from saved rows using the unchanged local
  evaluator's read-only comparison path. The result is exactly identical to the
  returned comparison. This is not new inference, generation or final scoring.
- Preserve seven mandatory failed gates. Resource/inference passes and falling
  new-corpus monitoring loss cannot override them. Conversely, do not describe
  this outcome as an execution failure or claim there was no learning at all.
  The candidate remains a complete, unaccepted artifact.
- Confirm no production final request/declaration/comparison and development-only
  evaluator access records. Do not open final payloads, use unused budget, select
  a different checkpoint or propose automatic retries after this terminal failure.
- Keep historical Phase 17, the synthetic diagnostic and Phase 17.1 separate.
  There was no serious real-data replay here, so do not overstate restart evidence
  from the tiny checks or earlier synthetic diagnostic. The historical kernel cause
  remains unidentified and the original failed recovered model remains unaccepted.
- Scope the next decision to owner-authorized prospective design. Low exposure and
  domain coverage are hypotheses, not diagnoses; no new threshold, architecture,
  data source, epoch count or run is selected or executed during closure.

Initial formatting findings were corrected. All independent reconstruction passes
completed successfully; the strengthened pass adds checks without changing any
experimental acceptance rule. Four focused rejection checks pass for digest
tampering, partial-final-update counting, nonfinite/rate mismatch and the separation
between execution, development quality and final acceptance. Actual results are in
[closure-validation.json](closure-validation.json). No full historical suite, model
training, hosted CI, physical Linux check or publication was performed.
